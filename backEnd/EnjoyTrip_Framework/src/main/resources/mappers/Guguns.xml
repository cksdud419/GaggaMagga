<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ssafy.project.model.dao.GugunsDao">
	<select id="selectGuguns" resultType="Guguns">
		SELECT *
		FROM guguns
		WHERE sido_code = #{sidoCode}
	</select>
</mapper>